/**
 * Widget type registry (docs/SPEC.md §2, §4): manifest-shaped entries, keyed by
 * source-qualified identity `(source, tag)`.
 *
 * Placeholder — no catalog yet; populated by the C-E1 epic.
 */
export {};

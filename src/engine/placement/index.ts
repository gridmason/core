/**
 * First-fit auto-placement, collision resolution, and size constraints
 * (docs/SPEC.md §2) over grid geometry `{x,y,w,h,i}`.
 *
 * Placeholder — no placement logic yet; populated by the C-E1 epic.
 */
export {};

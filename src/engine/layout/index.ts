/**
 * LayoutDoc model + resolution pipeline + 3-level governance (docs/SPEC.md §5):
 * host default → org published → user personal, most-specific-wins with locked
 * slots merging down. Exposes `resolveLayout(inputs) → EffectiveLayout` as a
 * pure function.
 *
 * Placeholder — no layout logic yet; populated by the C-E1 epic.
 */
export {};

/**
 * The gridstack.js binding (docs/SPEC.md §2): the `<gm-page-canvas>` custom
 * element, edit mode (drag/resize/add/remove/tabs + keyboard alternative), and
 * the per-widget error boundary. The only DOM consumer in the package — its
 * tests run under happy-dom (see vitest.config.ts).
 *
 * Placeholder — no canvas logic yet; populated by the C-E2 epic.
 */
export {};

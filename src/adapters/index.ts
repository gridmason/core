/**
 * Adapter interfaces (docs/SPEC.md §2) — interfaces only; the host implements
 * them: persistence, gates, permissions, telemetry, settings-form. Core makes
 * zero network calls and ships no backend (SPEC §1); the bundled dev-only
 * in-memory/localStorage persistence adapter lands with the C-E3 epic.
 *
 * Placeholder — no adapter interfaces yet; populated by the C-E3 epic.
 */
export {};
